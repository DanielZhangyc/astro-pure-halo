## 变更

-

## 验证

- [ ] 已运行 `pnpm check`
- [ ] 已运行 `pnpm build`
- [ ] 已检查受影响的 Halo 页面、空状态和错误状态

## 发布检查（仅 `release/*` / `hotfix/*`）

- [ ] `package.json` 与 `theme.yaml` 版本一致
- [ ] 分支名、版本号和 `CHANGELOG.md` 版本一致
- [ ] `release/*` 已包含最新 `dev`

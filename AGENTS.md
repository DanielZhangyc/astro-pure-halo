# Astro Pure Halo 项目约束

## 项目目标

本项目将 [Astro Theme Pure](https://github.com/cworld1/astro-theme-pure)
移植到 Halo。目标是在 Halo 中延续 Pure 的核心设计语言、主要布局和使用体验，
同时提供可靠的 Halo 集成；它不是对上游实现逐行、逐组件或逐像素的机械复制。

遇到目标冲突时，按以下顺序权衡：

1. 功能正确、数据安全、Halo 兼容性；
2. 可用性、可访问性和主流浏览器行为；
3. Pure 的核心视觉语言与用户可感知的交互一致性；
4. 可维护性、性能和实现复杂度。

## 上游参考

- 上游 Pure 是视觉和交互的首要参考，但不是唯一可接受的实现方式。
- 本地只读对照副本位于 `.reference/astro-theme-pure`。修改已有 Pure 页面或组件
  时，应优先查看其中相关源码、样式和资源，避免仅凭印象复刻。
- 只读取与当前任务有关的上游实现即可；文案、数据映射、构建配置、Halo 集成及
  明显不影响界面的改动不要求逐项对照上游。
- 仅在任务确实需要较新上游内容时，才在对照副本中执行 `git pull --ff-only`。
  不得在该副本中保存本项目的移植改动。

## 移植与适配原则

### 有直接对应的 Pure 功能

- 默认保留其信息层级、主要布局、视觉特征和关键交互。
- 优先复用上游的设计令牌、资源和交互思路，但允许采用更适合 Astro 构建产物、
  Thymeleaf 和 Halo API 的代码结构。
- 不为个人审美随意重设计；超出任务范围的视觉改动应避免。

### Halo 特有或上游没有对应的功能

- 可以按 Halo 的内容模型、路由、插件能力、后台配置和用户预期进行设计与适配。
- 新界面应尽量沿用 Pure 已有的字体层级、颜色、间距、圆角、图标和动效语言，
  无需虚构一个上游并不存在的完全一致实现。
- 优先采用平台原生语义和成熟交互，尤其是表单、评论、分页、搜索、错误反馈、
  键盘操作和可访问性行为。

### 允许偏离上游的情形

存在以下理由时，可以做范围受控的差异：

- Halo 的数据模型、模板能力、插件 API 或路由机制不同；
- 修复上游在当前浏览器或运行环境中的缺陷；
- 改善可访问性、响应式可用性、性能、安全性或内容完整性；
- 上游功能在 Halo 中不可用，或照搬会造成明显较差的体验；
- 用户明确要求新增、删除或改变某项行为。

选择差异方案时，应保留用户最容易感知的 Pure 特征，并采用最小、清晰、易维护
的改动。只有会影响用户体验、兼容性或后续维护的实质性差异才需要在交付说明或
项目文档中记录；无需记录纯模板语法或内部结构差异。

## 实施流程

1. 判断任务属于直接移植、Halo 适配还是项目新增功能。
2. 对直接移植或可见样式改动，定位并阅读相关上游实现；无需遍历无关文件。
3. 先实现当前任务所需的最小范围，避免顺带重构或统一无关组件。
4. 若偏离上游，说明具体约束和取舍，并检查是否仍符合 Pure 的整体设计语言。
5. 不确定的产品取舍优先沿用现有 Halo 移植行为；只有会显著改变用户体验时才
   请求用户决定。

## Git 与提交约束

- 每个提交必须保持原子化：一个提交只包含一个可独立说明、验证和回滚的逻辑
  改动，不得混入无关的格式化、重构、版本升级或生成物更新。
- 提交前必须检查工作区并区分用户已有改动；只暂存当前原子改动涉及的文件或
  区块，不得擅自覆盖、回滚或提交用户的其他改动。
- `main` 是只包含可发布版本的稳定分支；`dev` 是日常开发的集成分支。普通功能、
  修复、文档和重构分支必须从 `dev` 创建，并通过 Pull Request 合入 `dev`。
- 普通开发分支不得修改 `package.json` 或 `theme.yaml` 中的版本号。准备发布时，
  从最新 `dev` 创建 `release/vX.Y.Z`，在该分支统一更新版本号和
  `CHANGELOG.md`，再通过 Pull Request 合入 `main`。
- 线上紧急修复从 `main` 创建 `hotfix/vX.Y.Z`，完成修复、升版和变更记录后通过
  Pull Request 合入 `main`。发布完成后必须通过 `main` 到 `dev` 的同步 Pull
  Request 回灌，避免修复丢失。
- 禁止代理直接在 `main` 上提交或向 `main` 推送，也禁止代理执行任何直接合并到
  `main` 的操作（包括本地 `git merge` 和合并 Pull Request）。
- 禁止直接向 `dev` 推送。`main` 和 `dev` 都必须启用 GitHub Ruleset，并要求
  Pull Request、线性历史、必选状态检查、禁止删除和禁止强制推送。
- `main` 只能接收 `release/vX.Y.Z` 或 `hotfix/vX.Y.Z`；该限制由仓库中的
  Pull Request policy 检查强制执行。合入 `main` 后，以同版本 `vX.Y.Z` 创建
  GitHub Release，由 Halo 官方 CD 工作流构建并发布制品。
- 仓库只允许 Squash merge，避免功能分支的临时提交和 merge commit 进入长期
  分支。提交信息使用清晰、可独立回滚的祈使句。
- “提交”“发布”或“完成”等一般指令均不构成绕过分支规则、直接合并 Pull
  Request 或向受保护分支推送的授权。

## 验证要求

验证范围应与改动风险相称：

- 修改共享布局、主题变量、核心组件或响应式规则时，检查桌面端与移动端、浅色
  与深色主题，并与相关上游页面对照。
- 修改局部组件时，只需检查受影响的视口、主题和交互状态；无需机械执行完整
  组合矩阵。
- 修改数据源、模板语法、路由或 Halo 插件集成时，重点验证真实数据、空状态、
  错误状态、链接和构建结果。
- 纯文档或不可见的配置改动不要求视觉检查。
- 尽可能运行与改动相关的检查或构建。无法执行的验证应在交付时明确说明，不得
  声称已经验证。

## 完成标准

功能正确、相关检查通过、视觉与交互保持 Pure 的整体辨识度，并且所有有意义的
平台差异都有合理依据，即可视为完成。“与原版一致”只应用于实际对照并验证过的
范围；不要使用“完全一致”或“无偏差”等无法证明的表述。

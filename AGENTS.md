# Astro Pure Halo 项目约束

## 原版忠实度（最高优先级）

- 本项目的唯一视觉与交互基准是
  <https://github.com/cworld1/astro-theme-pure>。
- 原版仓库的本地只读对照副本位于 `.reference/astro-theme-pure`。实现或修改
  任何视觉组件前，必须直接读取其中对应的源码、样式和资源，不得仅参考当前
  Halo 移植代码。
- 需要更新对照副本时，在 `.reference/astro-theme-pure` 中执行
  `git pull --ff-only`；不得在该副本中制作或保存移植改动。
- 移植必须完全忠于原版 Pure。不得重新设计、简化、美化或加入任何个人创作。
- 不得擅自改变原版的布局、组件结构、字体、字号、字重、行高、间距、颜色、
  圆角、阴影、图标、动效、断点、明暗主题表现和交互状态。
- Astro 到 Halo 的改动仅限于数据源、模板语法、路由和 Halo 插件集成等适配；
  这些适配不得改变最终可见效果。
- 每项视觉实现都必须先定位原仓库中的对应组件和样式，并以原实现为依据。
  禁止凭印象复刻。
- 每次涉及页面或组件样式的修改，都必须对照原版完成桌面端、移动端、浅色和
  深色视觉检查。没有完成逐项对照，不得宣称移植完成。
- 若 Halo 的技术限制导致无法无偏差实现，必须记录具体差异及原因并向用户说明；
  不得自行设计替代方案。

以上规则适用于整个仓库，并覆盖一般性的设计偏好或实现便利性。
